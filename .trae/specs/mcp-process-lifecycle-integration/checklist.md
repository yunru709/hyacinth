- [x] `src/mcp/process-transport.ts` 已创建并实现 `Transport` 接口
- [x] `ProcessTransport` 能基于外部 `ChildProcess` 进行 JSON-RPC 按行读写
- [x] `src/mcp/lifecycle.ts` 中 `MCPServerManager.connect()` 调用 `ProcessManager.start()` 真正 spawn 进程
- [x] `MCPServerManager.disconnect()` 调用 `ProcessManager.stop()` 优雅停止（含超时强制清理）
- [x] `StdioClientTransport` 的引用已从 `MCPServerManager` 中移除
- [x] `src/gateway/factory.ts` 的 `createAgent` 接收 `supervisor?: LifecycleSupervisor` 参数
- [x] 每个成功连接的 `MCPServerManager` 被注册到 `LifecycleSupervisor`
- [x] `src/gateway/cli.ts` 调用 `createAgent(options, supervisor)` 传入 supervisor
- [x] 主进程 `SIGINT`/`SIGTERM`/`exit` 时，MCP 子进程被 `shutdownAll()`/`forceKillAll()` 统一停止
- [x] `tsc --noEmit` 编译通过无类型错误
- [x] `AgentComponents` 返回类型与现有调用方兼容（无破坏性变更）
