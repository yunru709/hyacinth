# Checklist
- [x] 备份已完成，且备份路径已记录。
- [x] WebUI `POST /api/sessions` 能创建 normal webui session。
- [x] WebUI `POST /api/sessions` 能创建 precise webui session。
- [x] WebUI 新建 session 后会自动切换后端 WebSocket session。
- [x] Sidebar 不会把未知 channel 的 session 默认显示为 TUI。
- [x] WebUI 提供 Normal/Precise 可视化切换入口。
- [x] Precise 模式切换使用 session-aware 行为并与当前 session 状态一致。
- [x] WebUI 提供清屏、工具折叠/展开、帮助的可视化入口。
- [x] 根项目 TypeScript 检查通过。
- [x] WebUI TypeScript 检查通过。
