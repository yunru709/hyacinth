# Hyacinth 模块依赖分析

> 生成日期: 2026-07-23
> 分析范围: `src/` 下所有模块目录及顶层 `.ts` 文件

---

## 整体分层

```
┌─────────────────────────────────────────────────────────┐
│                    入口 & 组装层                          │
│   index.ts  →  gateway (factory / cli / tui / server)   │
├─────────────────────────────────────────────────────────┤
│                    核心引擎层                             │
│     orchestrator (主循环)  ·  agents (子Agent)           │
│     bypass (旁路Agent)    ·  machine (流程引擎)           │
├─────────────────────────────────────────────────────────┤
│                    功能服务层                             │
│   context  ·  tools  ·  mcp  ·  skills  ·  channels     │
│   memory   ·  knowledge  ·  plugins  ·  hot-reload       │
├─────────────────────────────────────────────────────────┤
│                    基础设施层                             │
│   provider  ·  registry  ·  lifecycle  ·  rollback       │
│   dependency  ·  repair  ·  schedule  ·  multimodal      │
│   parser  ·  ui  ·  env  ·  update  ·  runtime           │
│   setup  ·  local-model  ·  world-engine                 │
├─────────────────────────────────────────────────────────┤
│                    零依赖基础层                           │
│   logging  ·  prompts  ·  evolution  ·  utils            │
│   types.ts  ·  event-store.ts                            │
└─────────────────────────────────────────────────────────┘
```

---

## 各模块详细依赖

---

### agents

> 子Agent注册、委托工具、配置加载。支持 delegate / adversarial / parallel 三种协作模式。

以 **orchestrator** 为基础支持 AgentLoop 的创建与输出处理,
以 **context** 为基础支持上下文组装与压缩,
以 **tools** 为基础支持工具注册、过滤注册表、沙箱化,
以 **memory** 为基础支持会话/事件/统计/摘要持久化,
以 **provider** 为基础支持模型路由与用户标识,
以 **prompts** 为基础支持提示词加载,
以 **registry** 为基础支持 AgentRegistry 注册表,
以 **setup** 为基础支持代理配置加载,
以 **dependency** 为基础支持代码依赖分析,

被 **gateway/factory.ts** 调用做 Agent系统的完整组装（注册表、内置Agent、委托工具）,
被 **hot-reload** 调用做 Agent配置文件的热重载,
被 **orchestrator/loop.ts** 调用做子Agent委托,
被 **tools/runtime-control.ts** 调用做运行时启停/销毁子Agent,

---

### bypass

> 旁路/后台Agent管理器，含陪伴型世界引擎。

以 **world-engine** 为基础支持世界状态存储、时间推进、关系模拟、环境快照,
以 **logging** 为基础支持日志记录,
以 **types.ts** 为基础支持消息和事件类型定义,

被 **context/router.ts** 调用做陪伴模式的上下文路由（CompanionRouter）,
被 **context/section-resolver.ts** 调用做注入类型定义,

---

### channels

> 多通道接口：TUI、HTTP Webhook、飞书/Lark、ClawBot。

以 **setup** 为基础支持配置管理、模型上下文窗口,
以 **env** 为基础支持通道环境信息,
以 **logging** 为基础支持日志记录,
以 **orchestrator** 为基础支持 OutputHandler 和 TurnInfo 输出处理,
以 **provider** 为基础支持 Provider 类型,
以 **memory** 为基础支持会话ID生成与管理,
以 **gateway/factory.ts** 为基础支持 AgentComponents 类型,
以 **runtime** 为基础支持默认配置获取,

被 **gateway/tui.ts** 调用做 TUI通道的创建与管理,
被 **gateway/server.ts** 调用做 HTTP Webhook通道的创建与管理,
被 **registry/channel.registry.ts** 调用做通道注册表管理,
被 **plugins** 调用做插件通道处理器的注册,

---

### context

> 分层上下文组装（Zone 1-5）、压缩编排、令牌计数、精确模式、上下文路由。

以 **types.ts** 为基础支持消息类型定义,
以 **evolution** 为基础支持 Git上下文条目,
以 **hot-reload** 为基础支持清单加载器热重载,
以 **provider** 为基础支持 Provider接口和ModelRouter,
以 **prompts** 为基础支持提示词加载与渲染,
以 **logging** 为基础支持日志记录,
以 **memory** 为基础支持陪伴模式会话管理,
以 **bypass** 为基础支持旁路注入解析与世界引擎,
以 **setup** 为基础支持人格引导与配置,

被 **agents/delegate-tool.ts** 调用做子Agent的上下文构建,
被 **orchestrator/loop.ts** 调用做主循环的上下文组装与压缩,
被 **gateway/factory.ts** 调用做上下文相关模块的组装,
被 **hot-reload** 调用做上下文的重建,
被 **mcp/system.ts** 调用做MCP系统的上下文注入,
被 **plugins** 调用做插件上下文源注册,

---

### dependency

> TypeScript源码依赖分析器，解析符号定义与引用。

以 **evolution** 为基础支持 Git管理,
以 **utils** 为基础支持项目Key生成,

被 **agents/delegate-tool.ts** 调用做子Agent的代码依赖分析,
被 **gateway/factory.ts** 调用做依赖分析器的初始化,
被 **orchestrator/loop.ts** 调用做主循环中的依赖查询,

---

### env

> 系统环境信息收集（OS、Shell、工具链等）。

以 **logging** 为基础支持日志记录,
以 **prompts** 为基础支持环境提示词加载,

被 **channels** 调用做通道环境信息展示,
被 **gateway/factory.ts** 调用做环境信息收集与构建,
被 **tools/channel-info.ts** 调用做通道信息工具,
被 **tools/system-info.ts** 调用做系统信息工具,

---

### evolution

> Git集成：文件变更追踪与管理。

**零跨模块依赖**（仅使用 Node.js API 和内部文件导入）,

被 **context** 调用做Git上下文条目构建,
被 **dependency/analyzer.ts** 调用做代码依赖的Git追踪,
被 **gateway/factory.ts** 调用做Git管理器初始化,
被 **orchestrator/loop.ts** 调用做主循环的Git状态查询,
被 **rollback** 调用做逐轮次回滚的Git操作,
被 **tools** 调用做Git工具的底层实现,

---

### gateway

> 入口层：CLI命令分发、TUI满屏终端、HTTP API服务、全局组装工厂。

以 **provider** 为基础支持所有LLM供应商的配置与路由,
以 **orchestrator** 为基础支持Agent主循环与规划器,
以 **memory** 为基础支持会话/事件/统计/摘要/记忆持久化,
以 **setup** 为基础支持配置管理、向导、模型默认值,
以 **runtime** 为基础支持运行时配置中心,
以 **logging** 为基础支持日志记录,
以 **channels** 为基础支持多通道（TUI/HTTP/飞书/ClawBot）,
以 **ui** 为基础支持斜杠命令、聊天日志、主题等TUI组件,
以 **event-store.ts** 为基础支持事件读取,
以 **local-model** 为基础支持本地模型下载与管理,
以 **tools** 为基础支持工具注册表、执行器、权限处理器,
以 **lifecycle** 为基础支持进程生命周期管理,
以 **env** 为基础支持系统环境信息,
以 **update** 为基础支持自更新功能（仅cli.ts）,
以 **context** 为基础支持上下文组装与压缩,
以 **knowledge** 为基础支持知识库,
以 **skills** 为基础支持技能注册表与内置技能,
以 **agents** 为基础支持Agent注册表与委托工具,
以 **mcp** 为基础支持MCP系统,
以 **evolution** 为基础支持Git管理器,
以 **dependency** 为基础支持依赖分析器,
以 **plugins** 为基础支持插件管理器,
以 **schedule** 为基础支持定时任务调度器,
以 **hot-reload** 为基础支持热重载管理器,
以 **rollback** 为基础支持回滚记录器与工具,
以 **machine** 为基础支持流程引擎（Todo / Spec 模式）,

被 **channels/builtin/http-webhook.ts** 调用做 AgentComponents 类型引用,
被 **index.ts** 调用做应用入口点的 runCli 启动,

---

### hot-reload

> 12+文件监听器：配置、工具、技能、Agent、MCP、插件、Provider、提示词、模型目录等变更即时重载。

以 **agents** 为基础支持Agent配置热重载,
以 **context** 为基础支持上下文重建,
以 **provider** 为基础支持Provider配置与模型目录热重载,
以 **tools** 为基础支持工具注册表和包热重载,
以 **skills** 为基础支持技能热重载,
以 **plugins** 为基础支持插件热重载,
以 **runtime** 为基础支持运行时配置热重载,
以 **mcp** 为基础支持MCP服务热重载,
以 **prompts** 为基础支持提示词缓存清理,
以 **setup** 为基础支持模型上下文窗口查询,
以 **logging** 为基础支持日志记录,
以 **types.ts** 为基础支持 AgentDefinition / SkillDefinition 类型,

被 **context/composer.ts** 调用做清单加载器懒加载获取,
被 **gateway/factory.ts** 调用做热重载管理器的组装与启动,

---

### knowledge

> SQLite FTS5 全文搜索知识库，带CJK分词、结构化标签匹配、文件系统监视。

以 **logging** 为基础支持日志记录,
以 **tools/interface.ts** 为基础支持 Tool 类型定义,

被 **gateway/factory.ts** 调用做知识库的初始化与工具注册,

---

### lifecycle

> 进程生命周期管理：子进程管理、优雅关闭、守护进程（exit code 42 自动重启）。

以 **local-model** 为基础支持本地模型注册表与桥接,
以 **provider/local-config.ts** 为基础支持本地Provider配置,
以 **tools/background-registry.ts** 为基础支持后台进程注册表,
以 **logging** 为基础支持日志记录,

被 **gateway** 调用做生命周期管理器的创建,
被 **mcp/lifecycle.ts** 调用做进程管理器,
被 **mcp/system.ts** 调用做MCP生命周期管理,
被 **local-model/model-bridge.ts** 调用做进程管理接口,
被 **orchestrator/loop.ts** 调用做主循环的生命周期管理,
被 **lifecycle-integration-test.ts** 调用做集成测试,

---

### local-model

> 本地模型支持：llama.cpp 集成、模型下载管理、llama-server 进程管理。

以 **provider/interface.ts** 为基础支持 Provider 接口与能力声明,
以 **lifecycle** 为基础支持进程管理与生命周期,
以 **types.ts** 为基础支持消息和工具定义类型,

被 **gateway/tui.ts** 调用做本地模型的TUI管理,
被 **lifecycle/index.ts** 调用做模型注册表与桥接再导出,
被 **provider/index.ts** 调用做 LlamaCppProvider 再导出,

---

### logging

> 模块级命名空间的日志记录器。

**零跨模块依赖**（仅使用 Node.js API）,

被 **几乎所有模块** 调用做日志记录——这是整个项目中依赖最广泛的模块,

---

### machine

> 流程引擎：状态机注册表、Todo模式（任务分解→定义→顺序执行）、Spec模式（需求→任务列表→验收）。

以 **logging** 为基础支持日志记录,
以 **prompts** 为基础支持流程提示词加载,

被 **gateway/factory.ts** 调用做流程引擎的初始化与注册,
被 **orchestrator/loop.ts** 调用做流程工具的检测与执行,
被 **tools/flow.ts** 调用做流程工具的注册,

---

### mcp

> Model Context Protocol 集成：MCP服务器生命周期、工具桥接、stdio/SSE传输。

以 **tools/registry.ts** 为基础支持工具注册表,
以 **context/composer.ts** 为基础支持上下文注入,
以 **lifecycle** 为基础支持进程管理与监督,
以 **logging** 为基础支持日志记录,
以 **types.ts** 为基础支持 MCPConfig 类型定义,

被 **gateway/factory.ts** 调用做MCP系统的初始化,
被 **hot-reload** 调用做MCP配置热重载,
被 **orchestrator/loop.ts** 调用做MCP桥接查询,
被 **plugins/manager.ts** 调用做MCP系统的插件管理,
被 **tools/runtime-control.ts** 调用做 mcp_status 运行时工具,

---

### memory

> 会话管理、对话持久化、事件存储、统计追踪、摘要存储、跨会话项目记忆、陪伴模式会话。

以 **types.ts** 为基础支持 Session / Message 等类型,
以 **logging** 为基础支持日志记录,
以 **utils** 为基础支持项目Key生成,

被 **agents/delegate-tool.ts** 调用做子Agent的对话与事件存储,
被 **channels** 调用做会话ID生成与管理,
被 **context/router.ts** 调用做陪伴模式会话管理,
被 **gateway** 调用做所有会话/事件/统计/摘要/记忆相关组件的初始化,
被 **orchestrator/loop.ts** 调用做对话存储、事件追加、统计更新,
被 **tools/runtime-control.ts** 调用做陪伴模式会话的运行时切换,

---

### multimodal

> 多模态支持：图片存储、注入、view_image 工具。

以 **types.ts** 为基础支持 MessageContent 类型定义,

被 **orchestrator/loop.ts** 调用做图片内容构建与注入,

---

### orchestrator

> 核心Agent主循环：上下文构建→LLM调用→流事件处理→工具执行→结果反馈→循环守卫。

以 **provider** 为基础支持所有LLM供应商、模型路由、用户标识,
以 **context** 为基础支持上下文组装、压缩、令牌计数、精确模式、路由,
以 **tools** 为基础支持工具执行器、注册表、包、结果缓冲、注入过滤,
以 **memory** 为基础支持对话/事件/统计/摘要存储,
以 **parser** 为基础支持输出路由（文本/思考/工具调用分流）,
以 **skills** 为基础支持技能注册表,
以 **mcp** 为基础支持MCP桥接,
以 **dependency** 为基础支持依赖分析器查询,
以 **agents** 为基础支持Agent注册表委托,
以 **lifecycle** 为基础支持生命周期管理,
以 **multimodal** 为基础支持图片处理,
以 **machine** 为基础支持流程引擎交互,
以 **schedule** 为基础支持心跳调度器,
以 **repair** 为基础支持循环守卫与工具风控,
以 **evolution** 为基础支持Git状态查询,
以 **rollback** 为基础支持逐轮次回滚录制,
以 **runtime** 为基础支持默认配置与运行时配置,
以 **setup** 为基础支持最大上下文令牌与安全配置,
以 **event-store.ts** 为基础支持事件追加,
以 **utils** 为基础支持文本内容提取,
以 **logging** 为基础支持日志记录,
以 **types.ts** 为基础支持消息类型定义,

被 **agents/delegate-tool.ts** 调用做子Agent的循环执行,
被 **channels** 调用做各通道的输出处理,
被 **gateway** 调用做主循环的创建与启动,
被 **tools/ask-user.ts** 调用做用户确认问题类型,
被 **tools/compression.ts** 调用做压缩触发,
被 **tools/runtime-control.ts** 调用做运行时的 AgentLoop 控制,

---

### parser

> LLM输出解析器：分流文本、思考、工具调用。

以 **types.ts** 为基础支持 StreamEvent 类型定义,

被 **orchestrator/loop.ts** 调用做LLM流式输出的解析与分流,

---

### plugins

> 插件系统：plugin.json 清单、工具/技能/MCP/Channel动态注册。

以 **tools** 为基础支持工具接口与注册表,
以 **skills** 为基础支持技能注册表,
以 **context** 为基础支持上下文源与组装接口,
以 **channels** 为基础支持通道处理器与配置,
以 **mcp** 为基础支持MCP系统管理,
以 **logging** 为基础支持日志记录,
以 **types.ts** 为基础支持 SkillDefinition / MCPConfig 类型,

被 **gateway/factory.ts** 调用做插件管理器的初始化,
被 **hot-reload** 调用做插件的热重载,
被 **registry/plugin.registry.ts** 调用做插件注册表管理,

---

### prompts

> 提示词模板加载与渲染。

**零跨模块依赖**（仅使用 Node.js fs/os/path API）,

被 **agents** 调用做Agent提示词加载,
被 **context** 调用做上下文提示词构建,
被 **env** 调用做环境提示词加载,
被 **hot-reload** 调用做提示词缓存清理与目录获取,
被 **machine** 调用做流程提示词加载,
被 **registry/skill.registry.ts** 调用做技能提示词加载,
被 **tools/runtime-control.ts** 调用做运行时提示词缓存清理,

---

### provider

> 14+ LLM供应商统一接口封装：重试、熔断、降级链、模型路由、能力目录。

以 **types.ts** 为基础支持 Message / StreamEvent / ProviderType 等核心类型,
以 **local-model** 为基础支持本地llama.cpp Provider,
以 **setup** 为基础支持配置管理,
以 **runtime** 为基础支持运行时配置,
以 **logging** 为基础支持日志记录,

被 **agents** 调用做模型路由与用户标识,
被 **channels** 调用做Provider类型,
被 **context** 调用做压缩模型路由与精确模式Provider,
被 **gateway** 调用做所有Provider的配置、管理、路由、目录加载（全面使用）,
被 **hot-reload** 调用做Provider配置热重载,
被 **lifecycle** 调用做本地Provider配置,
被 **orchestrator** 调用做主循环的Provider路由与模型选择,
被 **registry** 调用做ProviderRouter 和 Provider 类型,
被 **runtime** 调用做默认Provider配置,
被 **setup** 调用做Provider配置辅助,
被 **tools/runtime-control.ts** 调用做运行时Provider切换,

---

### registry

> 通用注册表系统：基于 GenericRegistry<T> 的7种专用注册表。

以 **types.ts** 为基础支持 AgentDefinition / SkillDefinition / ToolDefinition,
以 **prompts** 为基础支持技能提示词加载,
以 **channels** 为基础支持通道注册表,
以 **plugins** 为基础支持插件注册表,
以 **tools** 为基础支持工具接口与运行时控制,
以 **runtime** 为基础支持运行时配置中心,
以 **context** 为基础支持活跃路由器名称,
以 **provider** 为基础支持Provider路由与评估,

被 **agents/registry.ts** 调用做 AgentRegistry 的基类（再导出）,
被 **skills/registry.ts** 调用做 SkillRegistry 的基类（再导出）,
被 **tools/registry.ts** 调用做 ToolRegistry 的基类（再导出）,

---

### repair

> 自愈系统：循环守卫（死循环检测）、风暴抑制（工具调用频率控制）、工具调用清理。

以 **types.ts** 为基础支持 ToolCall 类型定义,

被 **orchestrator/loop.ts** 调用做主循环的安全防护,

---

### rollback

> 逐轮次Git回滚：记录每轮文件变更、支持回退到任意历史轮次。

以 **evolution** 为基础支持Git操作,
以 **logging** 为基础支持日志记录,
以 **tools/interface.ts** 为基础支持Tool类型定义,

被 **gateway/factory.ts** 调用做回滚系统与工具的初始化,
被 **orchestrator/loop.ts** 调用做每轮次的文件变更录制,

---

### runtime

> 运行时配置中心：集中化配置管理，变更通知，供所有模块运行时重配置。

以 **provider** 为基础支持本地配置、默认Provider、用户标识,
以 **setup** 为基础支持配置管理器类型,

被 **channels** 调用做默认配置获取,
被 **gateway** 调用做运行时配置中心初始化与全量配置,
被 **hot-reload** 调用做配置监听与热重载,
被 **orchestrator/loop.ts** 调用做运行时配置获取,
被 **registry/tool.registry.ts** 调用做工具注册表的运行时配置,
被 **schedule** 调用做调度器的运行时配置,
被 **setup** 调用做默认配置获取,
被 **tools** 调用做工具的运行时配置管理,

---

### schedule

> 任务调度器：Interval / Cron / Daily / Fixed-time / Random 调度，持久化到配置，支持通道路由。

以 **setup** 为基础支持 ScheduleConfig 类型,
以 **runtime** 为基础支持 RuntimeConfigCenter 类型,

被 **gateway/factory.ts** 调用做心跳调度器初始化,
被 **orchestrator/loop.ts** 调用做主循环的心跳调度,
被 **tools/runtime-control.ts** 调用做运行时调度管理,

---

### setup

> 系统设置：配置管理、模型默认值、API Key映射、安装向导、人格文件引导。

以 **provider** 为基础支持本地配置、默认Provider、模型目录加载器,
以 **runtime** 为基础支持默认配置,
以 **logging** 为基础支持日志记录,

被 **agents** 调用做代理配置,
被 **channels** 调用做通道自动检测、配置管理、模型上下文窗口,
被 **context** 调用做上下文配置与人格目录,
被 **gateway** 调用做配置管理、向导、模型默认值、人格文件（全面使用）,
被 **hot-reload** 调用做模型上下文窗口查询,
被 **orchestrator/loop.ts** 调用做安全配置与令牌限制,
被 **provider** 调用做配置管理,
被 **runtime** 调用做配置管理器类型,
被 **schedule** 调用做调度配置,

---

### skills

> 技能系统：技能注册表、内置技能、Skill工具。

以 **tools/interface.ts** 为基础支持 Tool 类型,
以 **types.ts** 为基础支持 SkillDefinition 类型,
以 **registry/skill.registry.ts** 为基础支持 SkillRegistry 基类（再导出）,

被 **gateway/factory.ts** 调用做技能注册表与工具的初始化,
被 **hot-reload** 调用做技能热重载,
被 **orchestrator/loop.ts** 调用做技能注册表查询,
被 **plugins** 调用做技能注册表与定义的插件管理,
被 **tools/runtime-control.ts** 调用做运行时技能管理,

---

### tools

> 工具体系：17+内置工具、35+运行时控制工具、工具注册表/执行器/包/权限/过滤、Python桥、交叉引用索引。

以 **orchestrator** 为基础支持 AgentLoop 和 AskUserQuestion 类型,
以 **provider** 为基础支持Provider路由与模型路由,
以 **runtime** 为基础支持运行时配置与配置模式,
以 **logging** 为基础支持日志记录,
以 **env** 为基础支持系统环境信息收集,
以 **utils** 为基础支持 diff 计算与项目Key,
以 **evolution** 为基础支持Git操作,
以 **machine** 为基础支持流程引擎交互,
以 **skills** 为基础支持技能注册表,
以 **context** 为基础支持活跃路由器切换,
以 **agents** 为基础支持Agent注册表,
以 **mcp** 为基础支持MCP系统,
以 **memory** 为基础支持会话管理（动态导入）与陪伴模式,
以 **schedule** 为基础支持定时任务调度,
以 **prompts** 为基础支持提示词缓存清理,
以 **types.ts** 为基础支持工具核心类型定义,

被 **agents/delegate-tool.ts** 调用做工具过滤、沙箱化、执行,
被 **gateway** 调用做工具系统的完整初始化（注册表、执行器、内置工具、权限处理器）,
被 **hot-reload** 调用做工具与工具包的热重载,
被 **knowledge** 调用做 Tool 接口类型,
被 **mcp** 调用做工具注册表桥接,
被 **orchestrator/loop.ts** 调用做工具执行、包管理、结果缓冲、注入过滤,
被 **plugins** 调用做工具接口与注册表类型,
被 **registry/tool.registry.ts** 调用做工具注册表基类,
被 **rollback** 调用做 Tool 类型定义,
被 **skills/skill-tool.ts** 调用做 Tool 类型定义,

---

### ui

> TUI终端界面组件：斜杠命令、聊天日志、Markdown渲染、Diff展示、主题。

以 **logging** 为基础支持日志记录,

被 **gateway/tui.ts** 调用做终端界面的所有UI组件渲染,
被 **gateway/factory.ts** 调用做命令注册表初始化,

---

### update

> 自更新系统：GitHub Release下载、本地源码编译安装。

**零跨模块依赖**（仅使用 Node.js API）,

被 **gateway/cli.ts** 调用做更新检查、下载、安装的全流程,

---

### utils

> 通用工具函数：文本提取、diff计算、项目Key生成。

**零跨模块依赖**（仅使用 Node.js API）,

被 **dependency/analyzer.ts** 调用做项目Key生成,
被 **gateway/cli.ts** 调用做项目Key生成,
被 **memory/session.ts** 调用做项目Key生成,
被 **orchestrator/loop.ts** 调用做文本内容提取,
被 **tools** 调用做 diff 计算和项目Key生成,

---

### world-engine

> 世界引擎：模拟状态存储、时间推进器、角色信息、关系系统、环境快照、世界工具。

以 **types.ts** 为基础支持 ToolDefinition 类型,

被 **bypass/agents/companion/index.ts** 调用做陪伴模式的世界模拟（使用并再导出所有组件）,

---

## 顶层文件

### types.ts

> 全局类型定义中心：Message、ToolCall、StreamEvent、Session、ProviderType、AgentDefinition、SkillDefinition、MCPConfig 等核心类型。

**零跨模块依赖**（纯类型定义文件）,

被 **几乎所有模块** 调用做核心类型定义——与 logging 并列项目中最广泛依赖的文件,

---

### event-store.ts

> 事件持久化：追加事件、读取最近事件。

**零跨模块依赖**（仅使用 Node.js API）,

被 **gateway/tui.ts** 调用做最近事件读取与展示,
被 **orchestrator/loop.ts** 调用做事件追加,

---

### index.ts

> 应用入口点：shebang到 runCli()。

以 **gateway/cli.ts** 为基础支持 CLI 启动,
以 **logging** 为基础支持启动日志,

**无其他模块依赖此文件**（它是应用的最顶层入口）,

---

## 依赖关系量化汇总

| 模块 | 依赖其他模块数 | 被依赖次数 |
|------|:---:|:---:|
| **gateway** | 24 | 2 |
| **orchestrator** | 20 | 7 |
| **tools** | 17 | 11 |
| **context** | 11 | 7 |
| **agents** | 10 | 4 |
| **hot-reload** | 15 | 2 |
| **plugins** | 8 | 3 |
| **mcp** | 6 | 5 |
| **lifecycle** | 5 | 6 |
| **channels** | 8 | 3 |
| **registry** | 8 | 3 |
| **memory** | 3 | 7 |
| **provider** | 5 | 14 |
| **bypass** | 3 | 2 |
| **skills** | 3 | 5 |
| **machine** | 2 | 3 |
| **setup** | 5 | 12 |
| **runtime** | 4 | 10 |
| **rollback** | 3 | 2 |
| **multimodal** | 1 | 1 |
| **parser** | 1 | 1 |
| **repair** | 1 | 1 |
| **schedule** | 2 | 3 |
| **ui** | 1 | 2 |
| **update** | 0 | 1 |
| **local-model** | 3 | 4 |
| **env** | 2 | 4 |
| **world-engine** | 1 | 1 |
| **dependency** | 2 | 3 |
| **knowledge** | 2 | 1 |
| **evolution** | 0 | 7 |
| **utils** | 0 | 5 |
| **prompts** | 0 | 8 |
| **logging** | 0 | 全员 |
| **event-store.ts** | 0 | 2 |
| **types.ts** | 0 | 全员 |

---

## 架构特征总结

1. **单点组装** — `gateway/factory.ts` 依赖了 24 个模块，是整个系统唯一的组装中心（依赖注入容器）
2. **无线电静默层** — `logging`、`prompts`、`evolution`、`utils`、`types.ts`、`event-store.ts` 零内部依赖，形成稳固的基础设施底层
3. **编排器中心** — `orchestrator` 依赖 20 个模块但不被任何其他模块作为"工具"调用——它只被上层 gateway/agents 创建和管理
4. **Provider 被使用最广泛** — `provider` 被 14 个模块依赖，因为所有需要调用LLM的地方都经过它
5. **环形依赖零** — 依赖关系呈严格的 DAG（有向无环图），无循环依赖
6. **工具系统为第二枢纽** — `tools` 被 11 个模块依赖，提供了整个系统的可执行能力接口

