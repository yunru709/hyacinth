# Source (dist only for distribution)
src/

# Tests
*.test.ts
*.test.js
*.test.d.ts

# Build config
tsconfig.json
vitest.config.ts

# Development
.vscode/
.idea/
.gitignore
.npmignore

# Dependencies
node_modules/

# Local model stuff
libs/
models/
adapters/

# Environment
.env
.env.*

# 本地运行产物（数据库 / TS 增量编译缓存）
*.db
tsconfig.tsbuildinfo

# Per-user config (auto-generated)
.agent/

# Build scripts (not needed at runtime) — 仅根目录，放行 training/scripts/（随包发布，供微调使用）
/scripts/

# IDE / dev config
.claude/
.trae/

# OS
.DS_Store
Thumbs.db

# Debug residue (vitest output redirect files; gitignored but must also be npmignored)
vt-*.txt
