/**
 * ## 内置 Workflow 导出
 *
 * 所有内置 Workflow 由对应的 YAML 文件编译而来。
 * TS 工厂函数已退役（仅保留文件用于参考）。
 */
export { getBuiltinYamlPath } from '../dsl/builtins.js';
export { BUILTIN_WORKFLOW_NAMES } from '../dsl/builtins.js';
export type { BuiltinWorkflowName } from '../dsl/builtins.js';
