{
  "name": "bootstrap",
  "description": "Bootstrap mode — initial setup and persona configuration",
  "triggerKeywords": ["bootstrap", "setup", "init", "configure"],
  "relatedTools": ["read", "write", "edit", "glob", "grep"],
  "phases": [
    {
      "name": "collect",
      "prompt": "## Bootstrap Mode — Configuration Collection\n\nYou are in Bootstrap phase. Your task:\n1. Check existing configuration in `~/.agent/`\n2. Collect missing configuration from the user\n3. Set up persona files if needed\n4. When configuration is complete, call `workflow step complete`\n\nCheck: config.json, personas/, workflows/, skills/",
      "allowedActions": ["note", "progress", "complete"],
      "onComplete": null
    }
  ],
  "complete": {
    "condition": "phase_complete",
    "phase": "collect"
  }
}
